*.beam
*.ez
*build
erl_crash.dump

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# Nix
result

# Embedded rebar3 Dialyzer PLTs
src/eparch/ffi/local/
src/eparch/ffi/deps.nix.new

# pre-commit
.pre-commit-config.yaml

# Example projects use a path dep on the parent library, so their
# lockfiles drift every time the root version bumps and provide no
# reproducibility value. Let each build regenerate them fresh.
examples/*/manifest.toml

# Extra
.env
